# Modin DataFrame Tuning - Default Configuration
# Sensible defaults with Ray backend

execution:
  engine_affinity: ray

data_types:
  dtype_backend: numpy_nullable

_metadata:
  version: "1.0"
  format: dataframe_tuning
  platform: modin
  description: Default Modin configuration with Ray backend
  generated_by: benchbox
