# Dask DataFrame Tuning - Distributed Cluster
# Configuration for multi-node distributed processing

parallelism:
  worker_count: 4
  threads_per_worker: 4

memory:
  memory_limit: "8GB"
  spill_to_disk: true
  spill_directory: /tmp/dask-worker-space

data_types:
  dtype_backend: pyarrow

io:
  memory_map: true

_metadata:
  version: "1.0"
  format: dataframe_tuning
  platform: dask
  description: Dask distributed cluster configuration
  generated_by: benchbox
