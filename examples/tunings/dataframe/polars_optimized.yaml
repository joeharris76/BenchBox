# Polars DataFrame Tuning - Performance Optimized
# Maximum performance for in-memory workloads

execution:
  streaming_mode: false
  engine_affinity: in-memory
  lazy_evaluation: true

memory:
  rechunk_after_filter: true

data_types:
  enable_string_cache: true

_metadata:
  version: "1.0"
  format: dataframe_tuning
  platform: polars
  description: Performance-optimized Polars configuration
  generated_by: benchbox
