# Polars DataFrame Tuning - Streaming Mode
# For datasets larger than available memory

execution:
  streaming_mode: true
  engine_affinity: streaming
  lazy_evaluation: true

memory:
  chunk_size: 500000
  rechunk_after_filter: false

data_types:
  enable_string_cache: true

_metadata:
  version: "1.0"
  format: dataframe_tuning
  platform: polars
  description: Polars streaming configuration for large datasets
  generated_by: benchbox
