# cuDF DataFrame Tuning - Performance Optimized
# Maximum GPU performance with memory pooling

gpu:
  enabled: true
  device_id: 0
  spill_to_host: false
  pool_type: pool

data_types:
  enable_string_cache: true

_metadata:
  version: "1.0"
  format: dataframe_tuning
  platform: cudf
  description: Performance-optimized cuDF with GPU memory pooling
  generated_by: benchbox
